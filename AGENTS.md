# הנחיות עבודה בפרויקט

## מטרת המערכת וגבולות הממשקים

זהו MVP מקומי למערכת סימולציות קוליות מבוססות AI. ממשק המנחים מיועד ליצירה, עריכה, פרסום וצפייה בתוצאות; ממשק המשתתפים מיועד רק לקבלת תדריך מסונן ולביצוע סימולציה. יש לשמור על הפרדה מלאה בין המידע והיכולות של שני הממשקים.

## מבנה מרכזי

- `src/pages/admin` — מסכי מנחים; `src/pages/participant` — מסכי משתתפים.
- `src/components`, `src/features`, `src/layouts` — רכיבי UI, תהליכים ופריסות.
- `src/repositories` — שכבת התמדה והגישה היחידה ל־`localStorage`.
- `src/services` — שירותי יישום, סינון מידע ואינטגרציות.
- `src/types`, `src/data`, `src/styles` — טיפוסים, נתוני ברירת מחדל ועיצוב.
- `public/assets` — נכסים סטטיים.

## פקודות עבודה

הרצה משורש הפרויקט:

```bash
pnpm install
pnpm dev
pnpm test
pnpm build
```

## כללים מחייבים

- כל הממשק, ההודעות והנגישות הם בעברית ובכיוון RTL.
- רכיבי React אינם ניגשים ישירות ל־`localStorage`; משתמשים רק בשכבת `repositories`.
- ממשק המשתתפים מקבל רק `ParticipantSimulationView` מסונן, ולעולם לא את ישות הסימולציה המלאה או מידע פנימי למנחה.
- אין לחשוף בדפדפן מידע פנימי, `service-role key`,‏ `ElevenLabs API key` או כל סוד אחר. משתני `VITE_*` נחשבים ציבוריים.
- כל שינוי חייב לכלול בדיקות מתאימות, הרצת `pnpm test` והרצת `pnpm build` לפני סיום.
- אין לבצע מחיקת קבצים או נתונים, migration הרסני, פריסה או שינוי בשירות חיצוני ללא אישור מפורש.
- בסיום כל משימה יש להסביר בעברית פשוטה מה השתנה וכיצד לבדוק זאת.
